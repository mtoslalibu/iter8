apiVersion: v1
kind: Service
metadata:
  name: productpage-v2
  labels:
    app: productpage
    version: v2
    service: productpage
spec:
  ports:
  - port: 9080
    name: http
  selector:
    app: productpage
    version: v2
