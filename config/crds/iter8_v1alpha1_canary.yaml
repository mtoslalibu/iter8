apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    controller-tools.k8s.io: "1.0"
  name: canaries.iter8.ibm.com
spec:
  additionalPrinterColumns:
  - JSONPath: .status.conditions[?(@.type == 'ExperimentCompleted')].status
    description: Whether experiment is completed
    format: byte
    name: completed
    type: string
  - JSONPath: .status.conditions[?(@.type == 'Ready')].reason
    description: Status of the experiment
    format: byte
    name: status
    type: string
  - JSONPath: .spec.targetService.baseline
    description: Name of baseline
    format: byte
    name: baseline
    type: string
  - JSONPath: .status.trafficSplitPercentage.baseline
    description: Traffic percentage for baseline
    format: int32
    name: percentage
    type: integer
  - JSONPath: .spec.targetService.canary
    description: Name of canary
    format: byte
    name: canary
    type: string
  - JSONPath: .status.trafficSplitPercentage.canary
    description: Traffic percentage for canary
    format: int32
    name: percentage
    type: integer
  group: iter8.ibm.com
  names:
    categories:
    - all
    - iter8
    kind: Canary
    plural: canaries
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          type: string
        kind:
          type: string
        metadata:
          type: object
        spec:
          properties:
            analysis:
              properties:
                analyticsService:
                  type: string
                metrics:
                  items:
                    properties:
                      confidence:
                        format: double
                        type: number
                      enableTrafficControl:
                        type: boolean
                      name:
                        type: string
                      sampleSize:
                        format: int64
                        type: integer
                      stopOnFailure:
                        type: boolean
                      type:
                        type: string
                      value:
                        format: double
                        type: number
                    required:
                    - name
                    - type
                    - value
                    type: object
                  type: array
              required:
              - analyticsService
              - metrics
              type: object
            targetService:
              properties:
                baseline:
                  type: string
                canary:
                  type: string
              type: object
            trafficControl:
              properties:
                interval:
                  type: string
                iterationCount:
                  format: int64
                  type: integer
                maxTrafficPercent:
                  format: double
                  type: number
                onSuccess:
                  enum:
                  - baseline
                  - canary
                  - both
                  type: string
                stepSize:
                  format: double
                  type: number
                strategy:
                  type: string
              required:
              - strategy
              type: object
          required:
          - targetService
          - trafficControl
          type: object
        status:
          properties:
            analysisState:
              type: object
            assessment:
              properties:
                conclusions:
                  items:
                    type: string
                  type: array
              type: object
            conditions:
              items:
                properties:
                  lastTransitionTime:
                    type: string
                  message:
                    type: string
                  reason:
                    type: string
                  severity:
                    type: string
                  status:
                    type: string
                  type:
                    type: string
                required:
                - type
                - status
                type: object
              type: array
            currentIteration:
              format: int64
              type: integer
            lastIncrementTime:
              format: date-time
              type: string
            observedGeneration:
              format: int64
              type: integer
            trafficSplitPercentage:
              properties:
                baseline:
                  format: int64
                  type: integer
                canary:
                  format: int64
                  type: integer
              required:
              - baseline
              - canary
              type: object
          type: object
  version: v1alpha1
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
